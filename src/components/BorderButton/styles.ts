import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        height: 43,
        borderRadius: 20,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center'
    },
    text: {
        fontSize: 16,
        lineHeight: 18,
        textAlign: "center"
    },
    iconLeft: {
        marginRight: 8,
    }
})