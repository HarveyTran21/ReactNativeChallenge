import { RootStackParamList } from '@app/navigation/types';
import type { StackScreenProps } from '@react-navigation/stack';

export type PropsType = StackScreenProps<RootStackParamList, 'LoginPage'>;
